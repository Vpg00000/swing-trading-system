# Portfolio health review

## Current holdings

{holdings_summary}

## Tax status

{tax_statuses}

## Sector concentration

{sector_exposure}

Review the existing holdings as a set, not just individually: is there a
common thread in any recent weakness (sector-wide news, a single stock
dragging correlated names, macro factor)? Does anything here suggest a
thesis has broken (not just a rank drop, but an actual reason the original
buy case no longer holds)? Flag anything a hard Python rule wouldn't catch
on its own (e.g. thesis-level concerns short of a rank-based sell trigger).
Follow the output contract in system_prompt.md.
